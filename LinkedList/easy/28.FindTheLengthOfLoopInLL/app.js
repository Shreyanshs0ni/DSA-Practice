import arrToLL from "../../arrToLL.js";
import Node from "../../LinkedListNode.js";
import printLL from "../../printLL.js";

const lengthOfLoop = (head) => {
    
}

const arr = [1, 2, 3, 4, 6, 7, 8, 9];
const ll = arrToLL(arr);
printLL(ll);
